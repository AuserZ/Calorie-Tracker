services:
  eaten:
    image: docker.io/auserz/eaten:latest
    container_name: eaten-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    deploy:
      resources:
        limits:
          memory: 300M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"